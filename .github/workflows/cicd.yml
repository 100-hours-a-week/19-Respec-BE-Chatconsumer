name: Deploy Spring Boot (No deploy.sh in ZIP)

on:
  push:
    branches:
      - dev

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Gradle Build (Skip Tests)
        run: |
          chmod +x gradlew
          ./gradlew build -x test

      - name: Make ZIP (specranking-dev-chatconsumer.zip)
        run: |
          mkdir specranking-dev-chatconsumer
          cp -r build specranking-dev-chatconsumer/
          zip -r specranking-dev-chatconsumer.zip specranking-dev-chatconsumer

      - name: Copy ZIP to Bastion only
        run: |
          echo "${{ secrets.PRIVATE_SSH_KEY }}" > private.pem
          chmod 600 private.pem

          scp \
            -o StrictHostKeyChecking=no \
            -o UserKnownHostsFile=/dev/null \
            -i private.pem specranking-dev-chatconsumer.zip \
            ec2-user@${{ secrets.DEV_BASTION_HOST }}:/home/ec2-user/

      - name: Bastion runs deploy to Private EC2
        run: |
          ssh \
            -o StrictHostKeyChecking=no \
            -o UserKnownHostsFile=/dev/null \
            -i private.pem ec2-user@${{ secrets.DEV_BASTION_HOST }} \
            "
              scp -o StrictHostKeyChecking=no specranking-dev-chatconsumer.zip ec2-user@${{ secrets.DEV_PRIVATE_HOST }}:/home/ec2-user/ && \
              ssh ec2-user@${{ secrets.DEV_PRIVATE_HOST }} 'unzip -o specranking-dev-chatconsumer.zip && bash deploy.sh'
            "
